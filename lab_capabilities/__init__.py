"""Optional platform capabilities an experiment suite may use.

Suite Platform RFC §10: governance is a capability, not the spine. A suite opts
in by declaring it; a suite that does not declare it runs with none of this code
reachable — which is the difference between "optional" and "off by default".
"""
